apiVersion: v1
kind: Service
metadata:
  name: {{ serviceName }}
  namespace: {{ namespaceName }}
spec:
  selector:
    app: {{ appName }}
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: {{ port }}
      targetPort: {{ applicationPort }}
